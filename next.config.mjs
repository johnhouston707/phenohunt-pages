/** @type {import('next').NextConfig} */
const nextConfig = {
  // Output as static export is not possible with dynamic routes
  // We'll use server-side rendering for the tester routes
};

export default nextConfig;

